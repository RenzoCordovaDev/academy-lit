{
  "name": "lit-ecosystem",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["packages/*"],
  "devDependencies": {
    "lerna": "^7.0.0",
    "pnpm": "^8.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter=@academy-lit-components/* run dev",
    "test": "pnpm --filter=@academy-lit-components/* run test",
    "build": "pnpm --filter=@academy-lit-components/* run build"
  }
}